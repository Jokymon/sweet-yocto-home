name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      name: Checkout the repository
    - uses: Jokymon/yocto-docker-repo-build-action@7f316b09db569c99ff3f9a125a2a5ff7f079953b
      name: Yocto build
