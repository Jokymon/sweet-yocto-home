First step
==========

Make sure 'repo' is installed, then call

  repo init -u . -m manifest.xml
  repo sync

  TEMPLATECONF=`pwd`/sources/meta-homesweetpi/conf source sources/poky/oe-init-build-env

  bitbake core-image-minimal
